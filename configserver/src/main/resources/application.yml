spring:
  application:
    name: configserver
  profiles:
    active: native
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:\Users\hp\Downloads\ecom-microservices\configserver\src\main\resources\config

#in search-locations with classpath it does not refetch when we make changes when appln running

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: "*"